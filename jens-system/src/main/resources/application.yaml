server:
  port: 6753
spring:
  application:
    name: jens-system-service
  datasource:
    url: jdbc:mysql://localhost:3388/jens?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-flex:
  mapper-locations:
    - classpath*:/mapper/*.xml
